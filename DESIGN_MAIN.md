# DESIGN_MAIN.md — M2.0 前置波设计细则（v2，2026-09-12 用户第二轮反馈后修订）

> 触发：用户 2026-09-12 看五屏稿后给出三处修订（见 §0）。本文件是该波唯一设计真相源；品牌/动效基线见 DESIGN_T09.md。
> 依据：apple-design.skill（**最高优先**：响应/弹簧/手势/材质/字体）+ designer（色彩纪律/字阶/间距/反模式）+ claude-design（内容驱动）。
> 用户裁决（2026-09-12）：①主页加**当日饼状图** ②快记页＝B+C 混合 + 三处修改（两个输入 / 小数点 / 键盘重设计）③明细页**先按时间分类**再列条目

## 0. 本轮用户反馈 → 动作映射
| 反馈 | 动作 |
|---|---|
| ① 第一屏还可以，**要加当日饼状图** | §3.5 新增「今日支出构成」卡（T-10b） |
| ② 快记页：**留** 图标分类 / 额度联动 / 金边描边；**改** ①要两个输入（项目名称+数值）②键盘缺小数点（C 占位）③键盘丑、重设计形状、字体用净值大数字那款但小一号 | §4 全面重写为**单一混合方向** + 键盘设计规格（T-11） |
| ③ 明细页不错，但**必须先按时间分类** | §5 明确「月 → 日 → 条目」时间优先结构（T-12） |

## 1. 信息架构（v3：T-12c 导航层级重构，用户 2026-09-12 裁决）
```
导航壳 HomeShell（单一 Scaffold + IndexedStack）——底部 tab bar 三页平级，切换不压栈：
  [分析] 主页 = 分析/指导页（预算 + 今日可花 + 进度 + 今日饼图 + AI 位）
  [资产] 资产页
  [统计] 统计页 ── 顶部 [明细 ›] → 明细页（统计页子页）
底栏上方居中主按钮 [记一笔] → 压栈进快记页（快记页 = 分析页下一级，返回回分析页）
```
- 底栏（tab bar + 记一笔）**常驻固定**：任何 tab、任何滚动位置都可达主操作（apple-design §16「Where can I go」wayfinding）
- 快记页顶栏**不再**有资产/统计/回顾入口（资产/统计已是顶级 tab）
- **快记即正式**：确认即写 `is_draft=false`，立即进统计/预算/明细；草稿链路（回顾页 / 批量补类别 / 待完善徽章）已废除

## 2. 预算引擎（不变，见 T-10a 已验收实现）
手填月度收入 + 计划存款；`budget = 收入 − 存款`；`fixedDaily = budget ÷ 当月天数`（向下取整）；`remaining = budget − 已花`；`liveDaily = remaining ÷ 剩余天数`（含今天，向下取整，负值钳 0）；无预算记录 → 全部 `null`（引导态）

## 3. 主页规格（v2：新增当日饼图 + 固定底部操作条）
1. 顶栏：**月份按钮**（左上）+ 预算设置入口（右上）——**无 ‹ › 箭头**（2026-09-12 用户裁决移除）
   - **月份按钮**：`2026 年 9 月 ▾`（`elevated` 填充 + hairline 边、radius 12、高 40；文字 `ink`、尾部 ▾ `inkSecondary`）
   - **点击弹出月历 sheet**（与类别网格同语言，禁渐变/投影）：
     - 顶部拖拽把手 + sheet 面 `overlay` + 顶部圆角 18
     - 年份行：`‹ 2026 ›`（圆形 hairline 按钮；未来年份禁用）
     - **月份 3×4 宫格**：每格高 52 / radius 12 / hairline 描边；**当前月 = 金边描边 + 极淡金底 + 金字**；**未来月灰显不可点**（`inkSecondary` 40% + 禁用态）；历史月与当月可点
     - 选中即关 sheet 并切换主页数据（历史月只读，符合既有语义）
     - 触控 ≥48；reduce-motion 下 sheet 以淡入替代位移
2. **Hero**：eyebrow「今天还能花」+ 实时额度（**Playfair 衬线 + 金渐变**，tabular 数字）+ 副行「基准 / 剩余额度 / 剩 N 天」
3. 进度区：本月已花 / 预算可花 + 进度条（gold；超支满格转 `semanticExpense`）+ 百分比
4. 今日行：今日已花金额 + 笔数
5. **【新增】今日支出构成（用户裁决①）**：
   - 形态：**环形图（donut）**，外径 104、环宽 14，中心显示今日合计（tabular，14px，`inkSecondary` 标签 + `ink` 数字）
   - 图例：右侧竖排，最多 3 类 + 「其他」合并；每行 = 6px 色点 + 类别名 + 金额（tabular，右对齐）
   - 配色：沿用统计页**金系 6 阶色阶** + 中性灰兜底（与统计页同源，禁止彩虹）
   - **空态**：今日无支出 → 只显示一行「今天还没有支出」（不画空环）
   - 数据：今日**已确认支出**按类别汇总（draft/收入/转账不计，T-05 口径）
6. **底部固定条**：`记一笔`（金渐变实心，规格同 §4 确认键）+ `明细`（金描边）
7. AI 卡位（M2.0 填充，不放假数据）——置于滚动内容末尾
8. 引导态（无预算）：Hero 换「先设置本月预算」卡 + 预算 sheet + 「沿用上月」

## 4. 快记页（v2：B+C 混合单一定稿，T-11 按此落地）
### 4.1 结构（v3：分类改 3×3 全显后重算垂直预算，仍免滚动）
| # | 元素 | 规格 |
|---|---|---|
| 1 | 顶栏 | `‹` 返回 + 「记一笔」+ 右侧 支出/收入 切换（默认支出） |
| 2 | **项目名称输入**（用户裁决②-1） | `elevated` 填充 + hairline 边，radius 12，**高 44**，hint「项目名称 · 如 瑞幸咖啡」；输入内容**喂既有解析器词典/历史联想** → 自动建议类别（复用 `SmartParser`，不新建逻辑） |
| 3 | **金额显示** | **Playfair Display 600 / 42px**（用户裁决②-3：净值同款字体、字号小于 hero 58）+ 右侧「清空」文字动作（`inkSecondary` 12px，金额为 0 时隐藏） |
| 4 | 额度联动行（用户保留项） | 细行（上下 hairline，**padding 6**）：`记这笔后，今天还能花 ¥71.4`——数值 `goldAccent`，其余 `inkSecondary`；无预算时整行隐藏 |
| 5 | **类别选择 = 3×3 网格**（用户 2026-09-12 裁决：**不要横向滑动、放大、一眼看全**） | 9 类固定 seed 一次全显（餐饮/交通/购物/居住/娱乐/学习/医疗/人情/其他）；每格 **58px 高 × ~101 宽**，图标 19px + 标签 11.5px；未选 = hairline 描边 + `inkSecondary`；**选中 = 金边描边 + 极淡金底 + 金字**；**禁止横向滚动**（用户明确要求） |
| 6 | **键盘（重设计，见 §4.2）** | 4 行 × 3 列含小数点；**键高 56**（由 64 让位，仍 ≥48 触控线） |
| 7 | 确认键 | 高 54，radius 16，**金边描边 + 极淡金底 + 金字**（用户保留项，非实心渐变） |

**页面结构（2026-09-12 T-11b 帧证据确认）**：外层 `Column[ 顶栏固定, Expanded(中段可滚), 确认键固定 ]`——**确认键永远钉在屏内**（「3 秒一笔」的可达性保障），<748 逻辑高视口下滚动的是键盘/金额区而非确认键

**垂直预算让位表（748px 屏幕，管理层实算）**
| 让位项 | 改动 | 省 |
|---|---|---|
| 分类横条 → 3×3 网格 | 46 → 178（+132） | **−132** |
| 键盘键高 | 64 → 56（4 行） | +32 |
| 金额行字号 | 44 → 42px | +4 |
| 项目名称行高 | 46 → 44 | +2 |
| 额度联动行 padding | 8 → 6 | +4 |
| 顶栏 padding | 17/12 → 15/11 | +3 |
| 页面 padding | 13/16 → 11/14 | +4 |
| 段间距 | gap 10 → 8（5 处） | +10 |
| 确认键高 | 56 → 54 | +2 |
| **合计** | | **−71（净）** |
- 结果：内容总高 ≈ 732 / 748，**无需滚动**；若实机仍显拥挤，第一档退让 = 键高 56→52（仍 ≥48）

### 4.2 键盘设计规格（用户裁决②-2/②-3；按 apple-design + designer 原则推导）
**为什么旧键盘丑**：14 个同规格圆角方块等权排列、宽扁比例 1.9:1、功能键与数字键无层级差 → 读起来像「教程里的计算器」。重设计方案：

| 维度 | 决定 | 依据 |
|---|---|---|
| 键位 | **4×3 共 12 键**：`7 8 9 / 4 5 6 / 1 2 3 / . 0 ⌫` | 用户裁决②-2：加入小数点；**C 键移出键盘**（清空改由金额行右侧文字动作承担，可见可发现，不藏长按） |
| 几何 | 键宽 101 × **高 56**（内容宽 336、**横 gap 16 / 纵 gap 10**） | 4px 基准间距 + row-gap > col-gap 的光学平衡（designer §3.3）；高 56 为 T-11 垂直预算让位后的值（§4.1） |
| 圆角 | **17**（≈ 高度 30%） | 刻意取值：比列表卡 12 更软、比 pill 更收敛——「柔和石板」而非「玩具按键」；每个数值可辩护（apple-design §16 Craft） |
| 材质分层 | 数字键 = `elevated` 填充 + 无描边；**功能键（`.` `⌫`）= 透明 + hairline 描边**；确认键 = 金描边 + 极淡金底 | 打破「14 键等权」的单调（apple-design §12 材质编码层级；designer §3.5 表面阶梯 + hairline、无投影） |
| 字面 | 键面数字 **Playfair 600 / 26px** + tabular（**用户 2026-09-12 裁决：键面也要衬线**——原定系统 sans 被推翻；字号由 24 提到 26、字重 600 以补偿衬线细笔画，**实机若显瘦改 700**）；小数点与 ⌫ 等功能符号保持 **sans 21px `inkSecondary`**（符号用衬线损辨识）；金额行 Playfair 600 / **42px** | 用户裁决优先于 apple-design §15 的默认规则（该规则允许「有理由的覆盖」，品牌一致性即理由）；符号与数字分治。**注：数值以工单 T-11 为准（21/42），本表原写 22/44 系管理层笔误，已订正** |
| 触感与反馈 | 按下 `TouchedScale` 0.97（120ms）+ `selectionClick`；反馈在 **pointer-down** 即时发生 | apple-design §1 响应 / §10 手势细节；沿用既有 `motion.dart` |
| 留白 | 键区不与页面其他元素贴边；键块上下各留 ≥12px「间」 | designer §4.1 Ma：留白即材料，不是剩余空隙 |
| 禁止 | 投影、发光、渐变填充、循环动画、图标化键面 | designer §7 反模式；用户「AI 味重」反馈 |

### 4.3 保留清单（既有能力一项不许少）
键盘 3 秒一笔 · 混合输入解析（`瑞幸 15`）· 时段默认类别 · 高频 chip · 午餐内联提示 · 收入切换 · **快记即正式（`is_draft=false`，T-12c）** · 触感 + `reduce-motion` 全退化

## 5. 明细页规格（v2：时间优先，用户裁决③）
**唯一默认结构 = 时间层级：`月 → 日 → 条目`**（自上而下）
1. 顶栏：`‹` 返回 + 「明细」+ 右侧总笔数
2. **月份行**（第一层分类）：`‹ 2026-09 ›`，默认当月；切换即换整月数据
3. **日期分组**（第二层分类，时间倒序）：组头 eyebrow 式 `今天 · 9月11日` / `昨天 · 9月10日` / `9月9日`……；组头右侧显示该日合计（tabular）
4. **条目行**（第三层）：类别图标 + 商户/备注（空则类别名）+ 金额（支出 = `ink` 暖白 / 收入 = `semanticIncome`）+「待完善」草稿标记（**仅历史草稿**；T-12c 后不再产生新草稿）
5. 类型筛选 chips（全部/支出/收入）置于月份行右侧——**筛选只做过滤，不改变时间分组**
6. **明确禁止**：默认按类别或金额分组/排序（用户要求时间优先）
7. 点行 → 编辑 sheet：全字段（金额/类别/商户/备注/日期/类型）+ 删除（二次确认 → 墓碑）；草稿编辑后保持草稿态
8. 空态：`这个月还没有记录`
9. **组头「该日合计」口径（管理层 2026-09-12 裁决保留）**：= **该组当前可见行的净额**（收入 +，支出 −，含草稿），即「当日流水净额」——与统计/预算口径（排除草稿、只算支出）**故意不同源**。理由：明细页职责是**如实呈现账本**（草稿已带「待完善」标签可见），口径唯一化归属统计/预算引擎；且筛选后净额随之变化符合「所见即所算」。**此处语义已锁定，后人勿以「口径不一致」为由改写**

## 6. 资产页字体回归（不变）
净值大数字恢复 **Playfair 衬线 + 金渐变**（管理层误读更正）；其余数字保持 tabular sans。

## 7. 字体与金色章程（v2）
- **衬线（Playfair）允许处**：①主页 Hero 实时额度 ②资产页净值大数字 ③app wordmark（顶栏 / 启动画面）④**快记页金额行（44px）** ⑤**快记页键盘数字键面（600 / 26px）**——④⑤ 为新增（用户裁决②-3 及其补充：键面也要衬线）
- **金渐变定义处 ≤ 4**：①主页 Hero 实时额度 ②资产页净值大数字 ③**底栏「记一笔」主按钮**（T-12c Part A，用户 2026-09-12 裁决「金渐变实心」）④饼图金阶（图表豁免）；wordmark 用平涂 `goldAccent`；**快记确认键为描边式（非渐变，不计入）**
- **金色禁止**：正文、分隔线、大面积底、循环动画、发光/霓虹、键面填充

## 8. 底栏 + 「记一笔」（T-14b **终稿：T1 / P2 / D1**，2026-09-14 用户拍板）

> **用户拍板** = `T1 / P2 / D1`，并追加三条要求：① 「记一笔」**只要加号、不要文字** ② 按钮与底栏的**字体要更好看** ③ 顶栏按键**严格受尺寸约束**（不许超出顶栏范围）。
> 过程稿 `design/bottom_bar_preview_v2.html` 仅供追溯；**v1 三案（底栏上方通栏 / 卡片内 / 底栏居中）已作废，勿复用**。

### 8.1 归属与底栏（铁律，不随设计变）
- 「记一笔」**仅属分析页**：入口在**分析页顶栏**（P2）；资产/统计 tab **无任何记一笔入口**（不留空槽、不置灰、不可压栈）；快记页 = 分析页下一级，返回必落分析页
- 底栏**只放三个 tab**，**底栏内不再有任何按钮**；总高在三个 tab 间恒定；顶部 hairline

### 8.2 顶栏按键（P2 位置 + D1 形态 + **只要加号**）
- **形态**：圆环键 —— 1.25px `goldAccent` 环 + **矢量描边加号**（stroke 1.75 / 圆头线帽 / 16×16 / 光学居中）。**禁用字体里的「＋」**（各平台粗细与位置不一，是「看着不精致」的常见根因）
- **尺寸预算（硬约束，不许超出顶栏）**：顶栏总高 **56**（上 padding 8 + 内容行 **48**）；月份按钮高 40 保持不变并垂直居中；右侧 [＋键][⚙]：**视觉直径 36 · 触控热区 48×48**（热区透明外扩 6，不再放大视觉）；两键间距 4；左右内边距沿用 16 / 8
- **宽度校验**：`16 + 月份按钮(≈130) + 弹性间距 ≥12 + 48 + 4 + 48 + 8 = 266` ⇒ **320dp 极窄屏仍有余量（54）**，不会挤压或溢出；月份按钮在极窄屏压缩自身内边距，不动字号
- **无文字**（用户裁决）；**不加首次引导**（保持克制），只保留触感 + 按下微缩放
- **按下态**：环内渐入 `goldContainer`（120ms 一次性）；`reduce-motion` → 仅变色，无渐入

### 8.3 底栏三 tab（T1 金细线滑动）
- 图标：**自绘 1.25px 线稿** 20×20（分析＝上升折线 + 终点小圆 / 资产＝层叠双框 / 统计＝三根不等高竖线）——**禁用 `Icons.*` 通用件**
- 选中：图标 + 文字 `goldAccent`、文字 w600、**下方 16×1.5 金线**（切 tab 180ms 位移过渡；`reduce-motion` → 直接切换无位移）
- 未选中：`inkSecondary`、w500
- tab 高 56（图标 20 + 间距 4 + 文字行高 16 + 上下 padding 8）

### 8.4 字体（回应「更好看」）
- **引入项目自带 UI 字体：MiSans**（免费商用；含简体中文 + 拉丁；几何现代、字距匀、小字号清晰）→ **subset 到实际用到的字形**（预计 +0.6–1.5MB），在 `pubspec.yaml` 声明 family；**备选**（体积/授权异常时二选一，不混用）：HarmonyOS Sans SC（更圆润）/ Noto Sans SC（最中性）
- 底栏文字：**12 / w500（未选中）· w600（选中）/ 字距 +0.08em**（中文小字需放松字距才不挤）；数字一律 tabular
- 顶栏月份按钮文字**同步换 MiSans**（避免「工具栏一个字体、内容另一个字体」）
- **Playfair 仍只管品牌时刻**（Hero 额度 / 资产净值 / wordmark / 快记金额行 / 键面数字）——**tab 文字与按键文字不得用衬线**

### 8.5 验收断言（Part B 专属）
① 分析 tab 顶栏存在 `home_record_key`，资产/统计 tab **不存在**该键 ② 该键**无文字节点**（只有矢量加号）③ 顶栏总高恒定 56；键视觉直径 36、热区 ≥48；**320×640 极窄视口无溢出**（渲染断言）④ 底栏三 tab 总高恒定、选中金线 key 存在 ⑤ 字体断言：tabs 与月份按钮 family = MiSans；Playfair 仅出现在品牌时刻（grep + TextStyle 断言）⑥ 三个 tab 图标为自绘线稿（源码无对应 `Icons.*`）⑦ analyze 零错 + test 全绿 + Windows 实跑帧（Dart PNG + md5 清单入库）

## 11. 统计页（T-21 终稿：**两张图 + 月份联动 + 存款转资产**，2026-09-15 用户拍板）

> **用户拍板**：① **A 与 B 两个点子都要，做成两张图**（A 柱＋额度红线、B 双线趋势）② 加**月份按钮**并与主页/明细联动 ③ 存款转资产采用**月末提示卡＋手动确认**。
> ⚠️ 与旧实现的差异是全量的：旧日视图硬编码 7 天、横轴标签滑移、纵轴零刻度、收入线恒 0（诊断见 `TICKETS_M2A.md` T-21）—— 本票按本节重写统计页图表层。

### 11.1 页面结构（自上而下）
1. 顶栏：`统计` 标题 + **月份按钮**（`2026 年 9 月 ▾`，复用主页同一套月历 sheet，不新造组件）+ 右侧「明细 ›」入口（保留）
2. 周期 chips：日 / 月 / 年（保留）
3. **图 1「每日支出 · 额度对照」（A 型，柱）**
4. **图 2「收支趋势」（B 型，双线）**
5. 类别占比 donut（保留）
6. 汇总卡：保底收入 / 临时收入 / 支出 / 可花预算 / 净结余（保留并补齐"保底 vs 临时"两行）

> 两张图**同页上下排列**（管理层定：A 在前回答"哪天超了"，B 在后回答"收支走势"）；若改成一键切换的单图模式，只需改此处一句 + 冻结后同步执行层。

### 11.2 图 1（A 型：柱 + 额度基线 + 临时收入点）
- **x 轴数据点**：日 = **所选月每一天**（28–31 根柱）；月 = 该年 12 个月（支出/收入成对柱）；年 = 数据中出现的年份（成对柱）
- **柱**：不超标部分 = `elevated` 填充 + `hairline` 边；**超出当天/当月额度的部分 = `semanticExpense`**（日视图按"日额度"判，月视图按"月预算"判）
- **额度基线**：水平 1px `goldAccent` 虚线（dash 4/3），值 = 可花预算 ÷ 周期天数（日视图 = ÷当月天数）；**无预算记录时不画基线**，并在图上方一行 `inkSecondary` 引导「先设置本月预算」
- **临时收入标记**：该周期内有 `type=income` 流水时，柱顶上方 4px `semanticIncome` 圆点（同日多笔取合计并在点旁标数）
- **纵轴**：**必须有金额刻度** —— 4 档（0 / ⅓ / ⅔ / max），`inkSecondary` 10pt，格式 `¥1,200`（千分位，tabular）；`max = 该视图数据最大值 × 1.1`
- **横轴标签**：**标签 x 必须等于对应数据点的 x**（整数 index，禁止 `length / 6` 这类非整数 interval）；标签数 ≤ 7（密度抽样：日视图每 5 天一个，月视图每 2 月一个，年视图每年一个）；末尾点必标

### 11.3 图 2（B 型：双线趋势）
- **支出线**：`semanticExpense`，线宽 2，数据点 ≥ 14 时不画点
- **收入线**：`semanticIncome`，线宽 2；**收入 = 保底收入均摊 + 临时收入**
  - 日视图：保底收入 = 月收入 ÷ 当月天数（常量基线），临时收入计入当天 ⇒ 线不再贴轴
  - 月视图：保底 = 该月 `incomeCents`，临时 = 该月 income 流水合计；年视图：按年汇总
  - **无预算记录**：收入线只含临时收入，并在图下一行 `inkSecondary` 说明「未设本月预算，收入线仅含临时收入」
- 纵轴金额刻度与横轴对齐规则同 11.2
- 空态（周期内零数据）：不画空线，显示「这个周期还没有记录」

### 11.4 月份联动（三页同源）
- 统计页月份按钮与主页、明细页共享**同一个 selected month 状态**；任一处切换，三处同步
- 日视图 = 该月每天；月视图 = 该年 12 个月；年视图 = 数据中出现的全部年份
- 未来月沿用既有语义（灰显不可选，见 §3.1）

### 11.5 计划存款 → 资产
- **数据模型（V3→V4 迁移，addColumn）**：`budget_months` 增两列 `savingsConfirmedAt` / `savingsSkippedAt`（nullable DateTime）；`AssetCategory` 增枚举值 `savings`（textEnum 存名字，旧行不受影响，无需数据迁移）
- **提示卡条件**：所选月**有计划存款且 > 0** 且 `savingsConfirmedAt == null` 且 `savingsSkippedAt == null`
- **位置**：分析页 Hero 下方；**当月最后 3 天**加一行语气提示「这个月快结束了」
- **两个动作**：
  - 「存进资产」→ 生成一条资产：`name = '9 月计划存款'`、`category = savings`、`valueCents = savingsTargetCents`、`purchasedAt = 该月最后一天 12:00`、`note = '由月度计划存款确认生成'`；同时写 `savingsConfirmedAt`；**资产页立即出现该条目**（与手记资产同一模型：可编辑 / 可卖 / CPD 照算）
  - 「这个月没攒够」→ 只写 `savingsSkippedAt`，**零资产落库**，该月不再提示
- **幂等**：同月只能确认一次（已确认时卡片不再出现；若重复调用则直接返回既有资产，不新建）
- **派生值铁律**：额度基线 / 收入均摊 **一律现算不落库**；本票新增落库字段只有上面两个"确认状态"时间戳

### 11.6 验收断言（T-21 专属）
① 日视图柱数 = **所选月天数**（28/29/30/31 各一例），且横轴标签 x **逐一对齐** index（结构化断言）
② 纵轴刻度存在且首/末值与 maxY 口径一致（金额格式化含千分位）
③ 注入「月预算 + 一笔临时收入」后：额度虚线存在、临时收入点存在、**图 2 收入线不再恒为 0**（结构化断言，不靠像素）
④ 月份按钮切换后统计页数据 = 该月数据，且与主页/明细同源（状态断言）
⑤ 存款确认：未操作时**零落库**；「存进资产」后资产页出现该资产（金额/类别/日期断言）且 `savingsConfirmedAt` 已写；「没攒够」后零资产 + 该月不再提示；重复确认不产生第二条
⑥ 空态 / 单点 / 极值（一月仅一笔）不崩；`analyze` 零错 + 207+ 单测全绿；帧 md5 唯一；**本票末出 APK**（M1 系列收官包）

## 12. 验收补充（§9 / §10 预留未用）
- T-10b：饼图数据与统计页同源（同一切面函数）；空态不画空环；固定底栏在任何滚动位置可见
- T-11：键位含小数点且 C 已移出；两个输入各自可测（名称→类别联想断言）；金额行用 Playfair 600/42px；**键面数字用 Playfair 600/26px**（用户 2026-09-12 裁决，实机显瘦升 700）；**小数点与 ⌫ 等符号保持 sans**（符号用衬线损辨识）；类别选择为 **3×3 全显网格、禁横向滚动**；垂直预算按 §4.1 让位表落地（免滚动，退让档 = 键高 56→52）；**保留清单逐项回归**
- T-12：默认视图必须是「月→日→条目」时间层级（断言：不管选哪个类型筛选，分组维度和顺序不变）
- T-12c：三 tab 平级切换不压栈（各带独立 key `tab_home`/`tab_assets`/`tab_stats`）；「记一笔」压栈进快记页且**返回落分析页**；明细唯一入口 = 统计页顶部「明细 ›」；快记确认即正式（`is_draft=false`）并立即进统计/预算/明细；月历 sheet 未来月灰显禁用、主页左右箭头已移除（无残余 key）；回顾页 / 批量补类别 / 待完善徽章**零残留**（含单测与 integration）
- 全局：analyze 零错误 + test 全绿 + Windows 实跑帧（Dart PNG + md5 唯一）+ release APK
