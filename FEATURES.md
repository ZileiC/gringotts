# Gringotts — 产品档案（FEATURES）

> 管理层维护。A 区 = 已确认；B 区 = brainstorm 裁决结果；C 区 = 负范围；P 区 = 备选池；T 区 = 技术路线；路线图随裁决更新。

## 产品身份
- **一句话**：AI 赋能的个人账本——记录每一笔开销、每一件资产，AI 帮你看懂钱、管住钱
- **平台**：Android 手机核心 / Windows 辅助（开发预览 + 后期 AI 分析与备份工作台）
- **数据观**：本地优先；账本数据不出设备，AI 请求仅由用户主动触发
- **受众**：自用为主，后期可上架
- **品牌（2026-09-08 用户定版）**：黑金 Gringotts 徽标（`brand/gringotts-logo.png`，md5 44ff10f0…）。设计语言从徽标反推：深黑层底 + 香槟金 accent + 品牌衬线字（仅品牌时刻），数据与正文用高可读无衬线。此徽标同时是 app 图标来源

## A 区 · 已确认功能

### A0 主页 = 分析/指导页（2026-09-11 用户实机反馈确立，M2.0 前置波）
> 用户原话：「主页面应该是分析页/指导页：根据输入的总收入和当前的总支出以及输入的预计要存的钱，算出该月当天可以花的钱；包括以后 M2.0 的 AI 分析、AI 建议都应该是这个页面做的；这个页面的功能和 UI 是真正的大头」
- **预算输入（用户裁决）**：每月手填一次「本月总收入 + 计划存款」，App 按月记住、随时可改；不自动跨月复制（新月份给「沿用上月」一键）
- **两种额度口径（用户裁决：两个都要）**：①固定基准额度 = (收入−存款) ÷ 当月总天数 ②实时剩余额度 = (收入−存款−已花) ÷ 剩余天数（含今天，向下取整）
- **页面元素**：Hero 今日可花（衬线金主数字）+ 基准/剩余/剩几天副行 + 本月进度条（超支转红）+ 今日已花 + 「记一笔」主 CTA + 「明细」入口 + **AI 分析位（M2.0 填充，不放假数据）**
- **AI 落点**：M2.0 的周期分析、理财建议、对话均在此页承载——布局一次成型，M2.0 只填内容
- 详细规格：`DESIGN_MAIN.md` §2/§3

### A1 资产档案页（对标「有数」，功能已核实）
- 一个页面记录所有财产：名称 / 分类属性 / 价值 / 照片
- 分类体系参考有数：硬通货（黄金、外币）、数码（手机电脑相机，折旧管理）、非标品（手办、奢品、游戏装备）、普通物品
- 资产状态：服役中 / 已退役 / 已卖出
- **B8 已采纳**：CPD 日均成本（8799 的 iPhone 用 999 天 = 日均 ¥8.8，进度条显示服役状态）+ 闲置变现复盘（买入-卖出差价、保值率、盈亏）+ 总资产净值看板

### A2 快速记账（2026-09-08 改向：手动智能速记为主 artery）
**设计哲学：3 秒一笔，先记后补，永不阻塞。**

到达路径（摩擦递减）：
- **主页 [记一笔] 按钮 → 速记页（2026-09-11 起：速记页不再是首页，首页为 A0 分析页）**
- 长按桌面图标 →「记一笔」直达键盘（B10 前半，M1.0）
- 下拉通知栏 Quick Settings tile → 直达（B10 前半，M1.0）
- 桌面小组件 Widget（B10 后半，M2.0）

输入层（只填一个数，其余全智能）：
- **金额是唯一必填项**；大号确认键一键入库
- 智能 1 · 时间段默认类别：早 7-9 早餐 / 午 11:30-13:30 午餐 / 晚 17-19 晚餐 / 深夜夜宵娱乐
- 智能 2 · 商户记忆：历史关键词联想，输「瑞」出「瑞幸」+ 自动带类别 + 常用金额
- 智能 3 · 一行混合输入：`瑞幸 15` 或 `15 瑞幸` 本地解析（regex + 词典，离线零成本）
- 智能 4 · 高频类别 chip 条：金额输入后横向一排近期高频类别，点选或直接确认采纳预填
- 智能 5 · 重复模式识别：工作日午间 ¥15±2 自动提示「这是午餐吗？」一键确认

先记后补机制：
- 速记以 draft 状态入库（金额+预填类别），类别/备注可后补
- 首页「今日 N 笔待完善」角标；当日回顾页批量补全
- 记录动作永不因「选类别」而中断——这是记账 app 死亡的第一原因

### A3 AI 能力（BYO：用户自选 provider + API key）
- A3.1 周期报表：日 / 月 / 年开销整理，图表 + 对应周期专业分析
- A3.2 经济学专家 skill：每日理财建议与专业指导，足够接地气（skill 后期由我们上网学习+借鉴生成）
- A3.3 消费对话窗口：花钱前可现场问专业建议
- A3.4 AI 分级调用（B6 采纳）：截图/速记解析用便宜快模型，月度分析用强模型

## B 区 · brainstorm 裁决结果（2026-09-08 用户裁决）

| 编号 | 功能 | 裁决 | 落位 |
|------|------|------|------|
| B1 | 预算系统 | ✅ 采纳 | M2.0 |
| B2 | 周期账单/订阅 | ✅ 采纳 | M2.0 |
| B3 | 账户体系 | ❌ **否决** | 入负范围 |
| B4 | 手动快速记账 | ✅ 采纳 | **M1.0 核心**（A2 设计全量） |
| B5 | 数据安全 | ✅ 采纳 | 导出 M1.0 / 加密 M2.0 |
| B6 | AI 分级调用 | ✅ 采纳 | M2.0（实现细节，执行层定） |
| B7 | 周报/月报 | ✅ 采纳 | M3.0 |
| B8 | CPD+变现复盘 | ✅ 采纳 | **M1.0**（并入 A1） |
| B9 | 多币种 | ❌ **否决** | 入负范围 |
| B10 | Widget/快捷入口 | ✅ 采纳（拆分） | 快捷入口 M1.0 / Widget M2.0 |

## C 区 · 负范围（明确不做）
- 银行/支付平台 API 自动拉流水（国内无开放 API、凭据风险高）
- 多人协作 / 家庭共享账本
- 股票基金实时行情与持仓联动
- 云同步 + 账号系统（本地优先；数据模型第一天就 sync-ready：UUID 主键 / updated_at / 墓碑删除）
- iOS 端
- **B3 账户体系**：不做账户/卡包/转账类型（用户否决 2026-09-08；数据模型不设 account 字段）
- **B9 多币种**：不做（用户否决 2026-09-08；金额字段只留整数分 + 默认 CNY 显示）

## P 区 · 备选池（不排期，等裁决门）
| 项 | 内容 | 裁决门 |
|------|------|------|
| 截屏记账 | 分享收图 → AI 解析 → 人工确认入库 | **M1.0 实测后裁决**：速记若好用则永不启用；不够则 M2.5 上 |
| 语音记账 | 「瑞幸十五块」语音解析 | 速记实测后看残余摩擦 |
| 剪贴板捕获 | 复制 ¥28.50 弹「记一笔？」（Android 10+ 剪贴板受限，可能不稳） | 实验性质，择机 |
| 常驻通知栏速记条 | 通知栏常驻「记一笔」动作 | B10 之外的补充入口，择机 |

## T 区 · 技术路线（管理层分解与确定）
- **T1 技术栈（已定）**：Flutter + Drift (SQLite) + Riverpod；Android APK 直装；Windows 桌面 = 开发预览 + 后期分析工作台
  - 理由：与 LifeOS 完全同栈，本机执行层环境已跑通全链路（LifeOS v0.1.2c 已实际交付 APK），零环境试错成本
- **T2 架构分层**：数据层（Drift）/ 服务层（账目服务、资产服务、速记智能、AI client）/ UI 层
- **T3 速记智能链路**：输入解析（regex+词典+历史联想，全部本地）→ 智能预填（时间段默认+高频 chip+模式识别）→ draft 入库 → 当日回顾批量补全；后期 AI 上线可增强解析，但速记核心 100% 离线可用
- **T4 AI client**：OpenAI-compatible（baseURL/key/model 三件套 BYO）；key 存 flutter_secure_storage
- **T5 图表**：本地聚合 + fl_chart；图先于 AI 存在（AI 负责「解读层」文字）
- **T6 金额铁律**：整数分存储，禁浮点（详见 AGENTS.md）
- **T7 同步（后期）**：手机 = 数据源，Win = 镜像；模型第一天 sync-ready
- **T8 版本语义（2026-09-08 用户定）**：里程碑 = M1.0 / M2.0 / M3.0 / M4.0；里程碑内调试修补 = M1.1 / M1.2 …；里程碑间大改 = M1.x 内累积或直接跳 M2.0
- **T9 执行环境**：Codex 专用 session（自动读 AGENTS.md）；git 仓库 = github.com/jharayden/gringotts（private，2026-09-08 已建）
- **T10 品牌 UI 重构窗口（2026-09-08 用户定调）**：用户对前端 UI 有高要求，整体重构安排在 **T-08 收官票**——M1.0 全部功能票（T-01~T-07）验收通过、功能全绿之后再换肤
  - 理由：功能先于皮肤，每屏只装一遍不返工；重构一次到位，重构后全量回归兜底（LifeOS 验证过的铁律：UI 必须有运行时证据）
  - 前置条件：自 T-02 起**强制 design tokens 层**（颜色/字体/间距一律经 lib/ui/tokens.dart，组件禁硬编码）——tokens 到位，T-08 重构 = 换 tokens + 局部布局打磨，而非全库翻找色值

## 路线图（2026-09-08 重排，M 编号制）
- **M1.0 本地核心账本**：智能速记全量（A2：直达入口×2 + 五层智能 + 先记后补）+ 资产档案（A1 含 CPD/变现复盘/净值看板）+ 分类分组 + 本地统计图表（fl_chart）+ CSV/JSON 导出 + **T-08 品牌 UI 重构（黑金换肤收官）**
- **M1.x**：M1.0 实测修补（编号 1.1 / 1.2 …）+ **截屏记账裁决门**（实测速记覆盖率后定生死）
- **M2.0 AI 上线**：BYO AI（B6 分级）+ 对话窗口（A3.3）+ 周期报表+AI 解读（A3.1）+ 预算（B1）+ 周期账单（B2）+ Widget（B10 后半）+ 本地加密（B5 后半）
- **M3.0 专家 skill**：每日理财建议（A3.2）+ 周报/月报（B7）
- **M4.0 Windows 端**：同步 + AI 分析工作台
