# HANDOFF_MANAGEMENT.md — 新任管理层 session 必读（一次性，读完即可开工）

> 本文档由上一任管理层（Hermes session）于 2026-09-12 撰写。项目档案在 `Desktop/Gringotts`（git 仓库 github.com/jharayden/gringotts，private）。
> **只需读一次**；日常开工只读 `PROJECT_STATE.md` + `TICKETS_M2A.md` + `WORKLOG.md` 顶部两条。

## 1. 你的角色与权限（用户亲自划定，越界=暴怒）
- **管理层（你）**：方向把控、brainstorm、派工、验收、GitHub 仓库管理（commit/push/建仓）。
- **代码只读**：你**不改任何代码**。发现缺陷 → 写进工单/WORKLOG 交执行层修，绝不亲手改。
- **执行层 = Codex**：在项目目录启动，自动读 `AGENTS.md`；它负责编码/构建/打包/自测/证据。
- 同一时间只允许一个 agent 改代码；你只动 `.md`。
- 用户是最终裁决者：设计/范围/观感的争议一律交用户；**技术判断**你可以定，但要有依据且可被驳回。

## 2. 双 session 协议
- 用户会在两个 session 间搬运 prompt：你出 prompt → 执行层干活 → 你验收 → 下一票。
- 文件即记忆：执行层只靠读文件获取全部上下文，**不要在 prompt 里复述档案内容**。
- 收工三连（执行层硬性）：WORKLOG → commit（`T-xx: <summary>`）→ push。**未提交/未推送视同未完工**。
- 每票一 commit，并在 WORKLOG 顶部记录 hash；你验收结论**必须标注所依据的 commit hash**。

## 3. 验收协议（五层，逐层给证据）
1. **记录核对**：commit 存在、hash 与申报一致、工作区干净（`git status`）
2. **独立复验**：`flutter analyze`（零问题）+ `flutter test`（全绿，看数量是否与申报一致）
3. **源码级审查**：读新增/改动的核心文件，**逐值对照设计 spec**（尺寸/token/算法）
4. **独立 integration**：亲跑该票的 integration 脚本（`flutter test integration_test/xxx.dart -d windows`），核对**确定性数值**是否与申报逐位一致
5. **UI 目检 + 数学复核**：截图核对界面；金额/百分比/天数**手算复核**
- **证据规则**：帧 md5 必须 run 内唯一；跨 run 差异若源于时钟/光标/动画中间态属正常；改动像素后 `git checkout -- evidence/` 还原执行层提交版；数值断言 > 像素帧（像素会被时机影响）
- 复跑前先备份证据（未提交的证据帧 `git checkout` 无法还原）

## 4. 成本纪律（本文件存在的主要理由）
用户用 DeepSeek API，**每票约 ¥15**，明确要求降本。降本手段（按杠杆排序）：
1. **常驻注入是最大开销**：`AGENTS.md` 每回合全文注入执行层上下文；`memory` 每回合注入双方。两者必须保持精简（目标：AGENTS ≤3KB；memory 只留跨项目行为铁律，项目细节一律进档案）
2. **执行层每 session 的必读量**：只让它读 `PROJECT_STATE` + `TICKETS_M2A` + `WORKLOG` 顶部两条。**设计文档只在需要时按章节读**（如「只读 DESIGN_MAIN §4.2」），不要整篇塞
3. **一票一 session**：session 越长，历史工具输出累积越多，越贵且越容易中断
4. **你自己的回合要短**：批量工具调用（一次 call 做多件事）；输出精简（报告只写结论+证据，不复述过程）；`terminal` 命令用管道过滤（`tail`/`grep`），不要 dump 全量日志
5. **证据按需**：确定性单测断言优先；截图/integration 只在**语义无法被单测证明**时才要求（每次 integration ≈ 20-30s + 一次人工核对）
6. **禁盲目重跑**（反浪费铁律）：同一动作最多重试 1 次且须由新事实驱动；失败即换确定性手段（单测/md5/grep）
7. **清理垃圾**：`build/`、`.dart_tool/`（可再生，本目录已清过 12GB）；证据帧本体可删，保留 md5 清单即够

## 5. 施工 prompt 写法（用户要求提升 prompt engineering）
**结构（8–15 行，要点式，不要散文）**：
```
继续 Gringotts 施工。<上一票> 已验收。先读 PROJECT_STATE.md + TICKETS_M2A.md 的 <票号>；需要时只读 DESIGN_MAIN §x。
本次任务：<票号 + 一句话>
要点：<3–6 条工单里最容易被误解/做漏的约束，其余细节让执行层自己看工单>
顺带项：<若有>
验收：<该票特有的逐条断言>
规则：反浪费（脚本先声明前提/固定时间戳）；收工三连 WORKLOG → commit → push；停下等验收。
```
**反例（烧钱形态）**：把工单全文抄进 prompt；复述数据铁律；写 30 行含全部背景；一次派多票；不写验收标准让执行层自由发挥。
**正例精髓**：prompt 负责「指路 + 防坑」，内容归档案；票越小越省（小票 5 分钟、大票拆两张）。

## 6. 路线图（用户已确认的方向；细节可在派工时细化）
### M2.0 前置波（当前，余 T-12b / T-13）
预算引擎 + 分析主页 + 快记重设计 + 明细页 —— 让 app「能用、顺手、看得懂」
### M2.0 正式：AI 上线（本波的价值兑现）
- **BYO AI 配置页**：provider / baseURL / apiKey / model 由用户自填（OpenAI-compatible 协议）；key 存 flutter_secure_storage
- **主页 AI 分析与建议**：落在主页 AI 位（现为占位卡）；**只喂聚合统计 JSON，不喂原始流水**（隐私+成本双保险）
- **对话窗口**：随买随问，带当前预算/资产上下文
- **周期报表 AI 解读**：图表本地出（fl_chart 已有），AI 只写解读文字
- **预算与周期账单**：预算系统、订阅/固定支出提醒
- **Widget + 本地加密**
### M3.0：专家 skill 体系
- 经济学/理财专家 skill（先上网学习借鉴再产出，版本化存 `ai_skills/*.md`）
- 每日接地气建议推送、周报月报、异常消费检测
### M4.0：Windows 端
- 同 Flutter 代码出 Windows 端 = 镜像 + 分析工作台；同步（先加密文件导出/导入，后局域网）
- 数据模型从第一天 sync-ready（UUID 主键 / updated_at / 墓碑删除）——已落实，勿破坏

## 7. 历史教训（拿真金白银与事故换来的，逐条都得守）
1. **管理层曾误 `reset --hard` 丢掉执行层已提交的修复** → 破坏性 git 操作前必须核对 `git log -3` + `git status` 并在 WORKLOG 备案；**git 历史修复须经用户知情**
2. **假证据三种形态**：同帧多名义（T-03）/ 前台残留造成假阳性（T-06 tile）/ 申报与实测不符（analyze 未重跑）→ 故有「验收必看源+亲跑+md5」三件套
3. **执行层两次「完工未提交」** → 收工三连已成硬性条款
4. **管理层的判断两次被驳回且驳回成立**（确认键是否固定 / git 历史误删）→ 分歧时以源码+帧+数值裁决，**不以角色服从**；驳回成立就撤回并公开记录
5. **口径必须一次定义清楚**（D1「总成本/天」定义含糊导致返工；明细页日合计口径已锁定）→ 写工单时把口径写死，别留解释空间
6. **设计文档内部会自相矛盾**（§4.2 vs §7 vs §8）→ 改 spec 时全文一致改，别只改一处
7. **evidence 帧会被回归重跑覆盖** → 回归帧固定落 `evidence/regression/`
8. **别用 debug 帧耗时当真机帧率** → 性能证据只认 profile 模式；真机判定权归用户
9. **执行层会在施工中途掉线，把整票施工留在干净工作区**（2026-09-13 T-12c 实况：四部分全部写完、零 commit）→ **保全铁律**：管理层一发现 WIP 立即 `commit + push` 保全（`WIP T-xx: …` 并注明未验收），保全后工作区干净、执行层可无缝续工；**掉线保险条款已写入 `AGENTS.md` 工作流程第 4 条**（每完成一个 Part 立即 `WIP T-xx: Part N` 提交，用户 2026-09-13 批准）
10. **批量删除前先分清「被跟踪」与「仅忽略」**（2026-09-14 管理层清理事故：误删 3 个已入库的 `.t02_*.png`，靠 `git checkout --` 救回）→ 规矩：删任何文件前先跑 `git status --porcelain --ignored`，`!!` 前缀＝仅忽略（可删/可重建），无前缀＝被跟踪（**要删必须先经用户裁决**）
11. **执行层会在失败测试上死循环烧钱/烧时间**（2026-09-14 T-14b：harness 反复重跑同一 integration 脚本，用户截断后 `DSH ACP: Internal error` 刷屏）→ **管理层防线**：① 派工 prompt 里**写死已知失败的精确位置与修法**（零重发现成本）② prompt 里**收窄重跑范围**（只跑被修脚本，禁止全量重跑）③ prompt 里加**熔断规则**：同一脚本最多 2 次且第 2 次须有新事实，第 3 次禁止——改为写 WORKLOG 停手报告 ④ 出现 harness 内部错误立即停手，不重试刷屏 ⑤ 每次中断后管理层先把 WIP 提交**push 保全**（本次两个 WIP 提交原本只在本地）

## 8. 当前开放项（接手即可开工）
1. ✅ **M1 系列已全部关闭**（T-10a → T-21，含全部追加修正）：**T-21 统计页图表重整 + 收入/存款语义**（`38235eb`）验收通过；交付包 `gringotts-T21-release.apk`（md5 `8cd0d04b…`）**待用户真机目测**
2. **当前开放票 = T-22（P3 微票，补证据）**：① 补 `evidence/t21/frame_md5.txt` 的 `[t05_stats_test]` 段（4 帧）② 补一张「有预算+有流水」的**填充态**统计页帧；只动证据脚本不动产品代码；可与下一票合并执行
3. **M2.0 正式波（AI 上线）—— ⏸ 按用户安排暂缓（2026-09-14）**：拆票表见 `TICKETS_M2A.md`「M2.0 正式波」（T-15 AI 基座 → T-16 主页 AI 建议 → T-17/T-18 → T-19 → T-20），T-15 启用条件 = 用户拍板 `design/ai_wave_preview.html` → 冻结 `DESIGN_AI.md`；**用户另有安排，未获指示前不派工、不催办**
4. **波内铁律（派工即生效）**：只喂聚合统计 JSON、**禁喂原始流水** / key 只进 `flutter_secure_storage` 且禁 log 禁 git / AI 调用必须可失败可降级 / 同数据缓存命中不重复请求 / 每票末重建 APK
5. **派工注意（deepseek harness 实测）**：其命令通道会吞非 CJK 非 ASCII 符号（U+00A5 等）→ prompt 提醒执行层在源码/测试里用 Dart 转义 `\u00a5`
6. 备用裁决池：截屏记账（M1.0 实测后定生死）、语音记账、剪贴板捕获
7. **流程条款**：验收记录里的挂账项必须写入下一张工单才算出账；执行层遗留未提交施工 → 先保全；**批量删除前先分清被跟踪/仅忽略**（§7 第 10 条）
