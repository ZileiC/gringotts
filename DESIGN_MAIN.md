# DESIGN_MAIN.md — M2.0 前置波设计细则（管理层发布，2026-09-11）

> 触发：用户 2026-09-11 实机测试反馈四条（见 §0）。本文件是该波唯一设计真相源，与 DESIGN_T09.md 并存（T09 = 品牌/动效基线仍有效；本文件 = 信息架构与页面重设计）。
> 依据：apple-design（动效/材质/字阶）+ designer（色彩纪律/反 slop）+ claude-design（内容驱动）。
> 用户四项裁决（2026-09-11）：①预算=每月手填（总收入+计划存款，按月记住可改）②额度=固定基准 + 实时剩余**两个都要** ③快记页整体推翻，先看 2–3 方向 ④明细页全字段可改 + 删除走墓碑。

## 0. 用户反馈 → 动作映射
| 反馈 | 定性 | 动作 |
|---|---|---|
| ① 主页应是分析/指导页（预算→每天可花多少钱），快记不该是主页；M2.0 的 AI 分析与建议也归此页 | 架构错误 | **§1 信息架构重构 + §2 预算引擎 + §3 主页规格**（T-10） |
| ② 主页点「记录」才进快记；快记页设计丑、AI 味重，整体重做 | 设计返工 | **§4 三方向选型**（T-11） |
| ③ 资产页很好，但「说好的字体」没了 | **管理层失误** | **§6 字体回归**（T-13） |
| ④ 已确认的收支无法查看/修改，需要能看到所有记录的页面 | 功能缺口 | **§5 明细页规格**（T-12） |

## 1. 信息架构重构（新的导航真相）
```
主页 = 分析/指导页（预算引擎 + 今日可花 + 进度 + AI 位）   ← 启动即此页
  ├─ [记一笔] → 快记页（二级，压栈）            ← 原主页降级
  ├─ [明细]   → 明细页（全部收支查看/编辑）      ← 新增
  ├─ [资产]   → 资产页（现状不变）
  ├─ [统计]   → 统计页（现状不变）
  └─ [预算设置] → 月度预算 sheet（收入 + 计划存款）
```
- 「首页即键盘、无导航层」这条 T-09 铁律**作废**（用户明确推翻）——改为「首页即分析页，记一笔一键可达」
- 快记页返回键回主页；记账成功后可留在快记页继续记（现状）或返回主页，落地方案见 §4

## 2. 预算引擎（数据 + 公式，纯函数可测）
### 2.1 数据模型（V2→V3 迁移）
新表 `budget_months`：`id` UUID 主键 / `year_month` TEXT（`YYYY-MM`，唯一）/ `income_cents` INT / `savings_target_cents` INT / `created_at` / `updated_at` / `deleted_at` 墓碑。
- 按月一条，用户可随时改；**不自动跨月复制**；当月无记录时主页显示引导卡（附「沿用上月数值」一键，写入新月份记录）
- 迁移 V2→V3：createTable，无历史数据回填（历史月份无预算是正常态）

### 2.2 口径（全部整数分；纯函数 `BudgetEngine`）
| 量 | 定义 |
|---|---|
| `daysInMonth` | 当月真实天数（28/29/30/31，按年判定闰年） |
| `remainingDays` | `daysInMonth − dayOfMonth + 1`（**含今天**） |
| `budgetCents` | `income_cents − savings_target_cents`（本月可花上限） |
| `spentCents` | 本月**已确认支出**合计（draft 不计、收入不计、transfer 不计——沿用 T-05 口径铁律） |
| `todaySpentCents` | 今日已确认支出合计 |
| `fixedDailyCents` | `budgetCents ÷ daysInMonth`（**向下取整**）——固定基准额度 |
| `remainingCents` | `budgetCents − spentCents`（可为负） |
| `liveDailyCents` | `remainingCents ÷ remainingDays`（**向下取整**）——实时剩余额度，负值钳制显示 0 |
| 进度 | `spentCents ÷ budgetCents`（>1 即超支，进度条满格转语义红） |

- 无预算记录时：三个额度字段返回 `null`，主页走引导态（不显示假数字）
- 边界必须单测锁定：跨月重置、2 月/闰年、当月最后一天（`remainingDays=1`）、超支负值、`budgetCents ≤ 0` 防御、draft 不计入

## 3. 主页规格（分析/指导页）
1. **顶栏**：`2026 年 9 月`（月份可左右切换查看历史月，只读）+ 右侧预算设置入口（icon）
2. **Hero 卡（页面主角）**：
   - eyebrow：`今天还能花`
   - **主数字：实时剩余额度 `liveDailyCents`——Playfair Display 衬线 + 金渐变**（品牌时刻，与资产页净值同语言；§7 金色章程修订）
   - 副行：`基准 ¥X/天 · 剩余额度 ¥Y · 剩 N 天`（tabular sans，次要色）
3. **进度区**：`本月已花 ¥2,340 / ¥3,600` + 进度条（gold；超支满格转 `semanticExpense`）+ 百分比
4. **今日行**：`今日已花 ¥53.6 · 3 笔`
5. **主 CTA**：`记一笔`（金渐变实心，规格沿用 §4 确认键）→ 进快记页
6. **次级入口**：`明细`（hairline 描边）
7. **AI 位（占位，M2.0 填充）**：低调卡位 `AI 分析与建议 · 即将上线`——**不放假数据**，只标明此页将是 AI 的落脚点（布局一次成型，M2.0 只填内容）
8. 引导态（无预算）：Hero 换成「先设置本月预算」卡 → 打开预算 sheet（收入 / 计划存款，两个数字键盘输入 + 保存）

## 4. 快记页重设计（整体推翻，三方向待用户选型）
**共同约束**：键盘式「3 秒一笔」不变；金额唯一必填；混合输入、时段预填、高频 chip、午餐内联提示、收入切换、draft 入库、`TouchedScale` 触感**全部保留**（这些是已验收资产）；差异在视觉语言与信息层级。
- **方向 A「素净计算器」**：零金渐变、零扫光。键面 = `surface` + hairline 分隔；分类 = 无底色文字 chip（选中仅 ink 描边）；确认键 = **暖白实心 + canvas 字**（不用金）；金额用 tabular sans 大字。气质＝一把好算盘。
- **方向 B「额度联动」**：键盘 + **记账即反馈**——金额下方细行实时显示「记这笔后今天剩 ¥70.4」（联动 §2 的 live 额度）；确认键 = 金描边 + 极淡金底（无扫光）；分类 pill 描边、选中 `goldContainer`。气质＝花钱即见重量。
- **方向 C「类别优先」**：上半屏类别图标网格（点选即高亮），下半屏金额键盘；无渐变、无扫光；选中类别 = hairline 框 + 淡金字。气质＝先想「这算什么」，再输数。
- 三方向均：无循环动画、无发光、无多余渐变；`reduce-motion` 全退化；触感保留
- 选型流程：用户看图拍板 → T-11 按选中方向落地（未选方向作废，不保留分支）

## 5. 明细页规格（T-12）
- **入口**：主页「明细」按钮 + 统计页（可选）
- **列表**：按日分组（今天 / 昨天 / M月D日），组头 eyebrow 式；每行 = 类别点 + 商户/备注（无则显示类别名）+ 金额（支出 = ink 暖白、收入 = `semanticIncome`）+ 来源标记（`待完善` 草稿 / 已确认）
- **筛选**：月份切换（‹ 2026-09 ›，默认当月）+ 类型 chips（全部 / 支出 / 收入）
- **每行可点** → 明细编辑 sheet（**全字段**）：
  - 金额（数字键盘，整数分）、类别（九类 + 自定义）、商户、备注、日期（date picker）
  - 类型：支出 / 收入（可改，转账占位不开放）
  - **删除**：二次确认 → 墓碑（`deleted_at`），列表即时移除；不物理删除
  - 草稿在此编辑后**保持草稿态**（转正式走既有回顾页批量确认，避免绕过「先记后补」哲学）
- 空态：`这个月还没有记录`
- 验收：全字段编辑单测（每字段独立断言 + `updated_at` 刷新）、删除墓碑语义（raw 行留存）、draft 保持、统计口径不受影响（编辑后的金额立即进入统计与主页额度）

## 6. 资产页字体回归（管理层失误更正）
- **事实**：用户 T-08 反馈「资产档案的数字字体挺漂亮的可以保留」= 指 T-08 预览稿里**净值大数字的 Playfair 衬线 + 金渐变**；管理层在 DESIGN_T09 §2 误读为「保持无衬线 tabular」，还加了「不含净值大数字」，导致换肤后该字体消失——**责任在管理层**，执行层照 spec 执行无误
- **更正**：资产页**净值大数字恢复 Playfair Display 衬线 + 金渐变**；其余数字（列表值、CPD 徽章、天数）保持 tabular sans 不变
- 主页 Hero 主数字（§3.2）采用同一语言，形成「品牌时刻 = 衬线金」的一致规则

## 7. 字体与金色章程修订（替代 DESIGN_T09 §5 的金色条目）
- **衬线金（品牌时刻）允许处**：①主页 Hero 实时额度 ②资产页净值大数字 ③app wordmark（速记页顶栏 / 启动画面）
- **金渐变定义处** ≤ 4：上述 ①②③ + 饼图金阶（图表豁免）+ 确认键（新方向若保留金底）；其余一律禁止
- 金色仍**禁止**：正文、分隔线、大面积底、循环动画、发光/霓虹

## 8. 验收补充
- 预算引擎：边界单测全套（§2.2 列表逐条）
- 主页：额度数值与引擎一致（同源单一函数）、无预算走引导态、月份切换只读、AI 位不出现假数据
- 快记：既有 9 个 integration 脚本中涉及速记页的断言**按新 IA 更新**（入口从主页进），其余速记能力断言保持通过
- 明细：§5 验收项 + 编辑后统计/主页额度即时联动
- 全局：116+ 单测保持绿；Windows 实跑帧（Dart PNG + md5 唯一）；`flutter analyze` 零错误；release APK 重建
